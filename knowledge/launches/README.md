# Launches

Launch/release plans produced by `/plan-launch`. One file per launch, named `YYYY-MM-DD-<name>.md`. Start
from `templates/launch-plan.md`. Include the post-launch review in the same file once complete.
